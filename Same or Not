#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
  int n,m;
    scanf("%d%d",&n,&m);
    int a[n],b[m],i,count=0;
        for(i=0;i<n;i++){
            scanf("%d",&a[i]);
            }
    for(i=0;i<m;i++){
        scanf("%d",&b[i]);
        }
    if(n==m){
        for(i=0;i<n;i++){
            if(a[i]==b[i]){
                count++;
                }
            else{
                count=0;
                break;
                }
            }

    if(count>1){
        printf("Same");
        }
        else{
            printf("Not Same");
            }
        }
    else{
        printf("Not Same");
        }
    return 0;
    }
    
                
